apiVersion: v1
kind: Service
metadata:
  name: aiops-agent
  labels:
    app: aiops-agent
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: aiops-agent
